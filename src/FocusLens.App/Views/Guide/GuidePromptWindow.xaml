<Window x:Class="FocusLens.App.Views.Guide.GuidePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ask FocusLens Guide" Width="440" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="False" FontFamily="{StaticResource BodyFont}">
    <!-- One line: "What do you need help with?". Enter starts the guide, Esc closes. -->
    <Border Margin="10" Height="56" CornerRadius="{DynamicResource CardRadius}" Background="{DynamicResource SurfaceBrush}"
            BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" ShadowDepth="4" Opacity="0.18" />
        </Border.Effect>
        <Grid Margin="16,0">
            <TextBlock x:Name="Placeholder" Text="What do you need help with?" Style="{StaticResource Muted}"
                       VerticalAlignment="Center" IsHitTestVisible="False" FontSize="15" />
            <TextBox x:Name="Input" BorderThickness="0" Background="Transparent" FontSize="15" VerticalAlignment="Center"
                     AutomationProperties.Name="What do you need help with?"
                     TextChanged="OnTextChanged" PreviewKeyDown="OnKeyDown" />
        </Grid>
    </Border>
</Window>
