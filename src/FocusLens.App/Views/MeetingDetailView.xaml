<UserControl x:Class="FocusLens.App.Views.MeetingDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctl="clr-namespace:FocusLens.App.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24" MaxWidth="860" HorizontalAlignment="Left">
            <Button Content="All meetings" HorizontalAlignment="Left" Command="{Binding BackCommand}" Margin="0,0,0,12" />

            <TextBlock Text="{Binding Title}" Style="{StaticResource Title}" />
            <TextBlock Text="{Binding Subtitle}" Style="{StaticResource Muted}" Margin="0,2,0,0" />

            <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                <Button Content="Re-summarize" Command="{Binding ResummarizeCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource NotConverter}}" />
                <TextBlock Text="{Binding Status}" Style="{StaticResource Muted}" Margin="12,0,0,0" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Notes -->
            <ItemsControl ItemsSource="{Binding Notes}" Margin="0,16,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                            <StackPanel>
                                <Grid>
                                    <TextBlock Text="{Binding Heading}" Style="{StaticResource Heading}" />
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="edited" Style="{StaticResource Muted}" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center"
                                                   Visibility="{Binding IsEdited, Converter={StaticResource BoolToVisibility}}" />
                                        <Button Content="Edit" Padding="10,3" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsEditing, Converter={StaticResource InvertedBoolToVisibility}}" />
                                        <Button Content="Save" Padding="10,3" Style="{StaticResource PrimaryButton}"
                                                Command="{Binding DataContext.SaveNoteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}" />
                                    </StackPanel>
                                </Grid>
                                <ctl:MarkdownText Markdown="{Binding Content}" Margin="0,8,0,0"
                                                  Visibility="{Binding IsEditing, Converter={StaticResource InvertedBoolToVisibility}}" />
                                <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="90" Margin="0,8,0,0"
                                         Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Action items -->
            <TextBlock Text="Action items" Style="{StaticResource Heading}" Margin="0,8,0,8"
                       Visibility="{Binding Actions.Count, Converter={StaticResource CountToVisibility}}" />
            <ItemsControl ItemsSource="{Binding Actions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsDone, Mode=OneWay}" Margin="0,4"
                                  Command="{Binding DataContext.ToggleActionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                  CommandParameter="{Binding}">
                            <TextBlock>
                                <Run Text="{Binding Owner, Mode=OneWay}" FontWeight="SemiBold" /><Run Text=": " /><Run Text="{Binding Text, Mode=OneWay}" />
                            </TextBlock>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Transcript -->
            <Expander Header="Transcript" Margin="0,20,0,0" Foreground="{DynamicResource TextBrush}">
                <ItemsControl ItemsSource="{Binding Transcript}" Margin="0,8,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="0,3">
                                <Run Text="{Binding Time, Mode=OneWay}" Foreground="{DynamicResource TextMutedBrush}" />
                                <Run Text="{Binding Speaker, Mode=OneWay}" FontWeight="SemiBold" />
                                <Run Text="{Binding Text, Mode=OneWay}" />
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
