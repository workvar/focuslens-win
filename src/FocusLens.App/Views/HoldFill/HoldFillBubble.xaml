<Window x:Class="FocusLens.App.Views.HoldFill.HoldFillBubble"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FocusLens hold to fill" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False" Focusable="False" IsHitTestVisible="False"
        FontFamily="{StaticResource BodyFont}">
    <!-- Hold to fill: shows what will be typed into the search field under the pointer, and a ring that
         fills while the pointer stays still. Click-through and never focused, like the ghost cursor. -->
    <Border Margin="8" Padding="10,8,14,8" CornerRadius="14" MaxWidth="340"
            Background="{DynamicResource PopupBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.18" />
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Grid Width="26" Height="26" VerticalAlignment="Center">
                <Ellipse Stroke="{DynamicResource SurfaceAltBrush}" StrokeThickness="3" />
                <Path x:Name="Arc" Stroke="{DynamicResource AccentBrush}" StrokeThickness="3"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                <Path x:Name="Check" Visibility="Collapsed" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2.2"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                      Data="M 8,13.5 L 11.5,17 L 18,9.5" />
            </Grid>
            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                <TextBlock x:Name="Caption" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" />
                <TextBlock x:Name="Suggestion" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"
                           TextTrimming="CharacterEllipsis" Visibility="Collapsed" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
