<Window x:Class="FocusLens.App.Views.Focus.FocusBlockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FocusLens block" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        FontFamily="{StaticResource BodyFont}">
    <!-- Laid over a blocked window. It stays until the user closes what it covers or leaves it.
         There is always a way out: ending the session. -->
    <Border CornerRadius="10" Background="{DynamicResource WindowBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="2">
        <!-- Shrinks to fit a small window instead of cutting off the buttons. -->
        <Viewbox StretchDirection="DownOnly" Margin="24">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16" Width="520">
            <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="44" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" />
            <TextBlock Text="Blocked to protect your focus" FontFamily="{StaticResource HeadingFont}" FontSize="32"
                       TextAlignment="Center" Margin="0,20,0,0" />
            <TextBlock Text="{Binding Message}" Style="{StaticResource Muted}" FontSize="17" TextAlignment="Center" Margin="0,16,0,0" />

            <Button Style="{StaticResource PrimaryButton}" Command="{Binding CloseTargetCommand}" HorizontalAlignment="Center"
                    Padding="18,10" Margin="0,24,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="{DynamicResource AccentTextBrush}" />
                    <TextBlock Text="{Binding CloseLabel}" Margin="8,0,0,0" Foreground="{DynamicResource AccentTextBrush}" />
                </StackPanel>
            </Button>
            <Button Content="End focus session" Command="{Binding EndSessionCommand}" HorizontalAlignment="Center" Margin="0,12,0,0" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                <TextBlock Text="&#xE916;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Live.RemainingText}" Style="{StaticResource Muted}" Margin="6,0,0,0" />
                <TextBlock Text=" left" Style="{StaticResource Muted}" />
            </StackPanel>
        </StackPanel>
        </Viewbox>
    </Border>
</Window>
